# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 프로젝트 개요

**정가 HBM ETF** — 팀 프로젝트(HBM_index)에서 만든 HBM 테마 커스텀 인덱스를 기반으로, **나만의 ETF를 설계**하는 개인 확장 프로젝트. 팀 완성본(중간발표 시점, 2026-07-25 스냅샷)을 동결 엔진으로 가져왔고, 그 위에 ETF 상품화 레이어를 얹는다.

- 원본 팀 레포: `D:\dev\HBM_index` (`완성본/` 폴더가 이 레포의 뿌리). **팀 레포와 git 연결 없음** — 팀 쪽이 바뀌어도 여기는 확정본 기준으로 독립 운영.
- Python 3.12 + 레포별 `.venv`. 한글 출력은 `PYTHONUTF8=1`로 실행.

## 자주 쓰는 명령어

```powershell
# 테스트 14종 (전부 오프라인·네트워크 불필요, 검증 237건)
Get-ChildItem tests\*.py | ForEach-Object { .venv\Scripts\python.exe $_.FullName }

# 개별 — 엔진 3종
.venv\Scripts\python.exe tests\test_v2.py                   # v2 방법론 명세
.venv\Scripts\python.exe tests\test_schedule_v2.py          # 스케줄러 스모크
.venv\Scripts\python.exe tests\test_develop_integration.py  # 통합
# 개별 — 가드 2종 (2026-07-27 점검에서 막은 구멍의 회귀 테스트)
.venv\Scripts\python.exe tests\test_universe_prescreen.py   # 사전 스크린 집행
.venv\Scripts\python.exe tests\test_backtest_calendar.py    # 거래일 정합·결측 처리

# 버퍼 정책 민감도 분석 (analysis/ 에 CSV 산출)
.venv\Scripts\python.exe analysis\sensitivity_v2.py
```

## 아키텍처

### 동결된 지수 엔진 (팀 완성본 — 원칙적으로 수정하지 않음)

```
src/universe.py → selection.py → weighting.py → index_calc.py
  (사전 스크린)       (선정)      (40/60+캡→IIF)   (M(t)=ΣIIF×FF×S×P)
src/rebalance.py — v2 방법론: 정원 폐지·히스테리시스 버퍼·무대체 수시변경.
                   비중은 weighting.py에 위임(assign_weights_v2 = 어댑터)
backtest/backtest.py — 이벤트 소비형 백테스트 (rebalance가 이벤트 생산,
                   backtest가 지수 재생. PR/TR 구분 — prices는 배당 미반영 수정주가여야 함)
```

- **현재 구성: 글로벌 13종목 단일 정본** (앵커 40 / 핵심 42 / 위성 18 — 앵커는 삼성 14.54·MU 13.04·하이닉스 12.42). `data/processed/구성표_글로벌확정_20260729.csv`. **국내/글로벌 두 판을 두지 않는다** — HBM 지수는 하나다(2026-07-29 사용자 결정). 12종목 `구성표_실사확정_20260725.csv`와 7종목 `구성표_실데이터_20260723.csv`는 역사 기록 — 기본값으로 쓰지 말 것.
- **해외 티커(MU) 시세는 `hist_data`의 KRW 환산 레이어**(`foreign_ohlcv_krw` — FDR 현지통화 × 환율)를 지나야 한다. 달력 정렬은 `align_foreign`이 **T-1 시프트**로 한다 — 미국 T 종가는 한국 T 마감보다 14시간 뒤에 나오므로 같은 날짜끼리 붙이면 룩어헤드다(2026-07-29 리뷰에서 잡음). FDR 직접 조회를 KRW 패널에 섞으면 통화 사고 — run_tracking/run_cu/run_capacity의 폴백은 해외 티커가 오면 일부러 실패한다(fail-closed). 코드 정규화는 `normalize_code`(무조건 zfill 금지).
- **산출 러너는 장 마감 후에 돌린다.** 2026-07-29 폭락장 장중 실행에서 같은 날짜 라벨에 소스별 가격이 12% 어긋났다(parquet 하이닉스 1,552,000 vs FDR 장중 1,374,000) — "같은 날 재실행으로 날짜 통일" 관행은 EOD 가격을 전제한다. 장중에 돌린 수치는 방향만 믿고 소수점은 마감 후 재확정할 것.
- `src/universe.py`는 팀 스냅샷에서 **빈 파일**이었다(2026-07-27 확인). selection.py가 "사전 스크린은 내 책임 아님"이라며 지목하는데 집행 코드가 없어서, 관리종목·자본잠식·의견거절 종목이 그대로 18% 비중으로 편입됐다. methodology.md 1장 표 그대로 구현해 채웠고 `scenario_min10.run_scenario()`가 호출한다 — selection.py·weighting.py는 무수정.
- `src/index_calc.py`는 이 레포에서 **아무도 import하지 않는다**(팀 하류 모듈 스냅샷). ETF 레이어의 지수 재생 경로는 `backtest.simulate_index`다.
- 방법론 근거 문서: `docs/methodology.md`, 제출 PDF들은 `docs/제출문서/`
- 엔진을 고쳐야 할 땐 팀 레포(`D:\dev\HBM_index`)와의 diff를 의식할 것 — 여긴 스냅샷이다.

### ETF 레이어 (`etf/` — 이 레포의 신규 작업, 5단계 전부 완료 2026-07-25)

모듈은 전부 "순수 로직(테스트 대상) + run_*.py(실데이터 러너)" 쌍. 산출 CSV는 `etf/output/`.

> **구성 진입점은 하나다** — `run_tracking.load_constituents()`. capacity·cu·compliance·benchmark 러너가 전부 여기서 구성을 받는다. 기본값은 `구성표_글로벌확정_20260729.csv`(**13종목 글로벌 정본**). 여길 옛 파일로 돌려놓으면 하위 4개 리포트가 통째로 폐기 구성을 산출한다(2026-07-26에 실제로 그 상태였음). 모든 리포트 머리글에 `source_line()`으로 출처를 찍으니, 리포트를 읽을 때 이 줄부터 확인할 것. ⚠ **아래 1~9번 본문 숫자는 12종목 시절 기록**이다 — 글로벌 정본의 재실행 수치(2026-07-29: 용량 1,728억·CU 30억 4.9bp·R1~R3 전 PASS·12.5y 갭 78bp·현금 25bp)는 S6 절과 설계서 PDF가 정본. 시장 데이터라 재실행할 때마다 소수점이 움직인다 — 문서 숫자를 고칠 땐 러너 전체를 같은 날 돌려 날짜를 통일할 것.

1. ✅ **추적오차** (`nav_sim.py` / `run_tracking.py`) — bt(level·turnover)에서 NAV 재생. TER은 달력일 일할, 드래그는 로그 항등 분해(합계=실측). 실측: TER45+비용30 = 갭 62bp/년, **현금 1%만으로 113bp** — 초강세 테마라 현금관리 > 보수. (현금 드래그는 지수 상승률 비례 — 7종목 시절 92bp에서 는 건 구성이 나빠진 게 아니라 12종목 지수가 더 올라서다.)
2. ✅ **용량** (`capacity.py` / `run_capacity.py`) — 용량 = min(허용일수×참여율×ADV/비중). 실측: **1,968억 원**(7종목 시절 613억), 병목은 와이씨켐(꼬마 비중)이 아니라 **넥스틴**(비중 3.25%×ADV 65억) — 12종목 재배분으로 넥스틴 비중이 10.6%→3.25%로 내려가며 3.3배 개선.
3. ✅ **CU/PDF** (`cu_design.py` / `run_cu.py`) — floor+현금(현금 ≥0 불변식), 0주 종목 검출. **권장 CU 20억**(20만 좌 — 관행 5~20만 좌의 상단), 총괴리 7.1bp ≤ 허용 15bp. 정밀도 병목은 **SK하이닉스 주가 176만원**(1CU 207주 → -6.9bp).
   - **총괴리는 현금을 목표 0인 포지션으로 세어 넣는다** (`DEFAULT_MAX_DEV_BP=15`). floor는 항상 미달 매수라 종목 편차가 전부 음수고 부족분이 통째로 현금에 쌓이므로, 현금을 빼면 지표가 실제 이탈의 **정확히 절반**이 된다. 그래서 옛 "10bp 허용치"는 실질 20bp였다 — 15bp는 완화가 아니라 강화다. 분해용 옛 값은 `stock_dev_bp`.
   - **권장은 '충족'이 아니라 '강건'에서 고른다** (`cu_robustness`). 오늘 종가만 보면 7억도 통과하지만 가격 ±5% 교란 300회에서 70%가 허용치 초과 — 반올림 격자 운이다. 15억도 3%대가 남는다. 300회 전부 통과하는 최소가 20억. seed 고정이라 재현된다.
4. ✅ **벤치마크 비교** (`benchmark.py` / `run_benchmark.py`) — 경쟁 ETF 실보유(KRX PDF, `.env` KRX_ID/PW 필요)에 판정 33종목 노출도 적용. 실측: **우리 순도 29.0% vs 국내 경쟁 11.8~22.3% = 1.3~2.5배** (국내주 HBM 상품은 사실상 유일 — PLUS 글로벌HBM은 해외형). 겹침 31~50%. ⚠ 표의 `1년 수익률` 열은 잣대가 다르다 — 자체는 오늘 확정 구성의 소급 재생(look-ahead), 경쟁사는 실제 거래 수익률. 순도·겹침만 동일 비교다.
5. ✅ **분산요건** (`compliance.py` / `run_compliance.py`) — 최종 12종목으로 **R1(≥10종목) PASS · R2(≤30%) PASS**, 최대비중 21.57%가 20% 강화방침에만 WARN. (7종목이던 초기 구성은 R1 FAIL = 상장 불가였고, 그 해소 과정이 6·7번이다.) 해소 방안은 remediation_notes 참조.

6. ✅ **종목수 10 시나리오** (`scenario_min10.py` / `run_scenario_min10.py`) — 팀 엔진(selection→weighting)을 임계값 컨텍스트로 재실행. **위성 임계 완화 단독은 무효**(공정·위원회 확인 통과가 현 7종목뿐 — 걸림돌은 실사 문서). 핵심 문턱 5%까지 내려야 12종목(순도 27.1%로 최저·규칙 정체성 훼손) vs **★S4 실사 확대(문턱 유지): 14종목 PASS·순도 28.8%** — 위성 상한 18%가 희석을 통제. S4여도 경쟁 ETF 순도(11.8~22.3%)보다 높아 "국내 1위" 유지.

7. ✅ **실사 확정 + 최종 구성** (`run_final.py`) — 사용자 판정(2026-07-25): 편입 5(오로스는 TSV 문서 근거로 인정)·관찰 2(심텍·SFA — "전망은 편입 요건이 아님"). **최종 12종목**: R1 PASS · 순도 29.0% · 용량 1,968억(3.2배) · CU 20억(7.1bp). 확정 판정 CSV = `data/processed/판정완료_20260725_실사확정.csv`, 구성표 = `구성표_실사확정_20260725.csv`.
8. ✅ **설계서 PDF** (`make_pdf_etf.py`) — `docs/정가HBM_ETF_설계서.pdf` 7쪽 (팀 투자설명서 스타일 계승).
9. ✅ **스트레스 테스트** (`stress_test.py` / `run_stress.py`) — 실측 MDD 구간(2026-06-18~07-24, -30.8% — 현재진행형 조정장)에서: 현금 1%가 상승장 +113bp 드래그 ↔ 하락장 **-424bp 방어**(부호 반전 실증), 하락 구간 리밸 비용 66bp로 증가. T2 유동성 가뭄: 용량은 ADV 정비례(×0.5 → 984억). **T2b 가정 민감도**(`stress_capacity_assumptions`): 참여율·청산일수는 관측이 아니라 우리가 고른 값이고 용량이 여기 정비례한다 — 10%·3일이면 1,968억 → **591억**(×0.30), ADV 반토막까지 겹치면 295억. T3 환매 내구성(ADV 반토막): AUM 1,000억이어도 일 20% 소화. 산출은 `stress_t1_tracking / t2_capacity / t2b_assumptions / t3_redemption.csv` 4개 (한 파일에 이어붙이면 헤더가 겹쳐 다시 못 읽는다).

**중요 — 이 프로젝트의 성격**: 팀 과제 제출용이 아니라 **사용자가 실제로 만들려는 자기 ETF**다. 모든 숫자는 험한 날 기준까지 검증하는 것을 원칙으로 한다(스트레스 테스트 상시화).

**미해결 (2026-07-27 전체 점검 기준)** — 사전 스크린 부재·비거래일 이벤트·결측 ffill·공휴일 처리는 해소됨.
- 티이엠씨 HBM노출도 0.00인데 편입(위성 요건은 메모리향≥70%라 규칙상 적법). 노출도 ≤5% 5종목이 비중 17.7%, 그중 주성엔지니어링은 노출도 3%에 비중 13.4%(2위) — 심사에서 나올 질문. **방법론 개정 사안이라 손대지 않았다.**
- 판정 CSV의 PER 0.00(넥스틴·오로스)은 산출불가를 0으로 인코딩한 것 — 선정 로직은 PER 미사용이라 계산엔 무해하나 표로 내보내면 오독.
- `benchmark_report.csv`는 **2026-07-26 스냅샷**이다. 07-27 재실행 때 KRX PDF가 어느 날짜로도 응답하지 않아(로그인 미성립) 갱신하지 못했다 — 나머지 산출물은 07-27 기준. KRX 복구되면 `run_benchmark.py` 재실행할 것.

다음 후보: 하락장 실측 계속 추적(조정장 진행 중), S4→정기변경 규칙 문서화, 관찰종목(심텍·SFA) 공시 모니터링.

## 알아둘 것

- 백테스트 가격 계약: **배당 미반영 수정주가**(PR). 공급처 수정주가가 TR 기준이면 결과가 부풀려짐 — backtest.py 도크스트링 참조.
- 회전율의 유일한 공식 수치는 `bt["turnover"]` (목표비중 비교는 drift를 놓침).
- `analysis/buffer_policy_sensitivity_v2.csv`: 버퍼 정책별 회전율·비용 드래그 비교 — wide(25%/65%)가 드래그 최소 6.7bp.
- pykrx import 시 "KRX 로그인 실패" 경고는 시세 조회엔 무해하지만 **ETF PDF 조회는 로그인이 필수**다 — 로그인이 안 붙으면 경쟁사 순도가 전멸한다. `run_benchmark`는 이때 리포트를 덮어쓰지 않고 예외로 멈춘다(직전 정상본 보존).
- 영업일이라고 ETF 보유내역 PDF가 있는 건 아니다 — 당일분은 장중·마감 직후 미공시라 빈 응답이 온다. `resolve_pdf_date()`가 기준 ETF로 실제 조회해보고 하루씩 뒤로 물린다.
- `fetch_adv`는 pykrx 거래대금이 실패하면 FDR 종가×거래량 근사로 폴백한다(경고 출력). 최근 실행은 계속 폴백 경로였으니 ADV는 근사값이다.
- `PR_NOTE.md`·`완성본_안내.md`는 팀 인계 당시 기록 — 역사 자료로 보존.

## 12.5년 장기 데이터 연결 (2026-07-27)

`D:\data`의 FnGuide 데이터셋(2014-01~2026-06, 4,267종목)을 붙였다. 기존 검증이
최근 1년(+207% 강세장)에서만 이뤄져 "험한 날" 표본이 없었던 한계를 푼다.

- `etf/hist_data.py` — parquet 로더. **상장 시점이 제각각**(2014년 6종목 /
  2018~2023년 6종목)이라 매 시점 살아있는 종목만으로 비중을 재정규화한다
  (`pit_weights`). 없던 종목을 소급 편입하면 그 자체가 룩어헤드다.
- `etf/run_stress_long.py` — 실제 위기 3구간에서 재측정.

### 결과 — 기존 숫자가 강세장 전용이었음이 드러났다 (2026-07-28 재실행)

| 구간 | 지수수익률 | 최대낙폭 | 현금1% 기여 | 종목수 |
|---|---:|---:|---:|---:|
| 전체 12.5년 | +3,728%* | -45.3% | +23bp(드래그) | 12 |
| 2018 반도체 사이클 붕괴 | -37.3% | -39.2% | **-72bp(방어)** | 7 |
| 2020 코로나 폭락 | -39.9% | -41.3% | **-329bp(방어)** | 7 |
| 2022 금리인상기 | -35.1% | -40.5% | **-59bp(방어)** | 11 |
| 2026 조정(진행중) | -31.8% | -38.6% | **-299bp(방어)** | 12 |

\* 수익률은 성과 주장에 쓰지 않는다 — 아래 '동일가중 반사실' 참조.

**용량이 위기에 붕괴한다 — 이것이 가장 중요한 발견이다.**

| 구간 | 용량 | 병목 |
|---|---:|---|
| 현재(정규장 기준, 07-28) | 1,766억 | 넥스틴 |
| 2022 금리인상기 | **187억** | 테크윙 |
| 2020 코로나 | **116억** | 디아이 |
| 2018 사이클 붕괴 | **74억** | 테크윙 |

기존 스트레스 테스트는 ADV를 ×0.5, ×⅓로 임의 축소했는데 **실측은 그보다 훨씬
가혹하다(1/10~1/27)**. 위기에는 낙폭보다 유동성이 먼저 마른다.
AUM을 정할 때 강세장 용량(1,766억)이 아니라 위기 용량(74~187억)을 기준으로 봐야 한다.

### 동일가중 반사실 (`etf/run_counterfactual.py`) — 수익률은 성과 주장에 쓰지 않는다

12.5년 수익률에는 사후선택이 세 겹 실려 있다: ① 2026년 HBM 판정 통과 기업만(생존
편향) ② 오늘의 유동시총 비중 = 이미 오른 종목에 소급 배정 ③ 상장 즉시 편입.
②의 크기는 잴 수 있다 — **같은 12종목·같은 재고정 규칙에서 비중만 동일가중**으로:

| 구간 | 오늘비중 | 동일가중 | 차이 |
|---|---:|---:|---:|
| 전체 12.5년 | +3,728% | +1,117% | **+2,612%p ← 소급 프리미엄** |
| 2024~현재 | +421% | +137% | +284%p |

MDD는 -45.2% vs -53.7% — 어느 비중이든 -45% 이상 깨진다. **낙폭·위기 용량·드래그가
믿어도 되는 숫자이고, 수익률은 어떤 형태로든 성과 주장에 쓰지 않는다** (①③은 이
표로도 못 잰다). 발표·문서에서는 수익률이 아니라 낙폭을 앞세울 것.
`simulate_reset_index`(hist_data)가 임의 target을 받는 순수 함수라 반사실 계산과
오프라인 테스트(test_hist_index)가 가능하다.

## 12.5년 융합 데이터 기준 최종 산출 (`etf/run_final_long.py`)

기존 러너들은 최근 1년(+207% 강세장)에서 쟀다. 같은 검사를 2014-01~오늘 전 구간에서
다시 돌린 결과가 아래다. `fetch_prices`·`fetch_adv`도 융합 데이터셋을 우선 쓰도록 배선했다
(네트워크 조회는 실행할 때마다 결과가 미묘하게 달라져 재현이 안 된다).

| 항목 | 기존(강세장 1년) | **12.5년 융합** (07-28 재실행) |
|---|---:|---:|
| 추적오차 갭 | 61bp/년 | **76bp/년** (TER 45 + 비용 7.8 + 현금 23) |
| 현금 1% 기여 | +92~113bp (드래그) | **+23bp** — 강세장 1년이 예외적으로 컸던 것 |
| 용량 (실측 거래대금) | 1,994억 | 2,329억 |
| **용량 (정규장 기준)** | — | **1,766억** ← 설계 기준으로 쓸 것 |
| CU 총괴리 | 20억 7.1bp | 20억 **9.8bp** · 7억 10.5bp — 정수 주 격자라 날마다 움직인다 |

(07-27 첫 산출 때 매매비용이 0.2bp로 나왔던 것은 회전율 누락 버그 — f3807a5에서
수정. 분기 재고정 사이 drift를 되돌리는 매매를 세지 않고 있었다.)

### 용량은 정규장 기준으로 본다

실측 거래대금에는 시간외·블록딜이 섞여 있는데, 그건 원할 때 원하는 만큼 체결되지 않는다.
"며칠 만에 청산 가능한가"를 묻는 용량 계산에는 정규장만 세는 편이 정직하다.
12종목 중 6종목이 배율 1.0 초과이고 최대는 삼성전자 1.83배다.
배율은 `D:\data\_live\volume_scale.csv`.

### 개인 복제 최소 투자금액 — 새로 계산

실제 ETF 상장은 자산운용사만 가능하므로, 이 구성을 재현하려면 12종목을 직접 사야 한다.
정수 주 단위라 총액이 작으면 비중이 어긋난다.

| 허용 편차 | 최소 투자금액 (07-28) | 실제 편차 | 현금 잔여 |
|---:|---:|---:|---:|
| 100bp | **약 1억 6,820만원** | 26.3bp | 0.26% |
| 50bp | 2억 4,390만원 | 8.5bp | 0.08% |
| 25bp | 3억 1,118만원 | 11.6bp | 0.12% |

하한을 만드는 것은 **SK하이닉스 155만원 × 비중 18.4%** 다. 소액으로는 이 한 종목의
정수 주 제약 때문에 비중이 크게 틀어진다.

⚠ 이 값은 **정수 주 격자라 가격 스냅샷에 민감**하다 — 07-27 산출은 6,829만원이었는데
하루 급락으로 2.5배가 됐다. 게다가 `min_replication_cost`의 이분탐색은 편차가 금액에
단조라고 가정하는데 실제로는 톱니다(더 작은 통과 금액이 존재할 수 있음). **자릿수로만
읽고, 정밀값으로 인용하지 말 것.** CU처럼 교란 강건 기준으로 고치는 것이 다음 후보.

(설계서 PDF는 07-28 재실행 숫자로 동기화됨 — `make_pdf_etf.py`는 CSV를 읽지 않고
하드코딩이므로, 산출을 갱신하면 PDF도 같은 날 재생성할 것.)

## S6 글로벌 확장 — 경로 B (2026-07-29 1차 실사 완료: MU 편입)

**8종 전원 판정 완료 (근거: `docs/글로벌실사_판정근거_20260729.md`)** —
편입 **MU 1종**(앵커, 규칙 0 — 노출도 불요가 유일하게 통했다).
제외: TSM·ONTO(HBM 언급 0회) · Advantest(메모리 테스터 ~20%, NAND 포함) ·
Resonac(반도체재료 세그먼트 통째 37.6%, HBM/NCF 분리 공시 없음) ·
BESI(하이브리드 본딩 매출 라인 미공시 — 수주 언급뿐).
관찰: **CAMT**(매출 기준 HBM % 미공시. 단 HBM 2사 수주 $260M/2년 ≈ 연매출의
~27% — 매출 전환·공시 시 30% 다툼권, 가장 유망한 관찰 종목).
글로벌판 **13종목·앵커 3사**(삼성 14.54/MU 13.04/하이닉스 12.42) —
**R3 20% WARN 해소**(최대비중 한미 18.00%), 순도 29.81%, 비앵커 10종 비중 불변
(40/60 버킷 분리 실증). 구조 교훈: 앵커는 '사실 확인'이라 국경을 넘고, 비앵커는
'비율 소명'이라 공시 제도·회사 다각화에 막힌다 — 글로벌 확장 ≈ 앵커 확장.
EDINET API 수집기는 키 등록(사용자 계정) 필요로 보류 — 2차 실사는 공식 IR
문서 수동 실사로 완료. venv에 pypdf·cryptography 추가(암호화 PDF 추출용).

국내 풀이 바닥났다는 신호(노출도 ≤5% 5종목이 비중 17.7%, R1 충족이 순도를
35.6→29.0%로 깎음)에 따라, **같은 규칙·같은 문턱으로 심사 대상만 해외로 확대**한다.
사용자 결정: 개인 투자 목적이므로 "국내 유일" 차별점보다 밸류체인 완결성 우선.

- `etf/global_candidates.py` — 후보 등록부 8종(MU·TSM·BESI.AS·0522.HK·6857.T·
  CAMT·ONTO·4004.T) + fail-closed 검증. **테마 가드**: 등록부에 없는 티커는 실사
  CSV에 있어도 거부(수요측 NVDA/AMD 등 제외 사유는 `EXCLUDED`에 코드로 기록).
  `normalize_code`가 zfill을 숫자 코드에만 적용('MU'→'0000MU' 사고 방지).
- `data/processed/글로벌후보_실사_20260729.csv` — **사용자가 채울 실사 템플릿**
  (가이드: `docs/글로벌실사_가이드.md`). 빈칸 = 편입 불가. `write_template`은
  기존 파일을 절대 덮어쓰지 않는다(채운 실사 보호). 단위: 시총·거래대금 억 원
  KRW 환산(환산기준일 기록), 노출도 0~1(35 쓰면 % 표기 의심 하드 에러).
- `etf/run_global_scenario.py` — 판정 완료 행만 국내 33종과 병합해 팀 엔진 재실행,
  국내판 vs 글로벌판 비교(구성·순도·R1~R3). 미실사 상태에서 글로벌판 = 국내판
  동일함을 기준선으로 검증했다. Micron이 실사 통과 시 규칙 0에 의해 **앵커 3사**가
  되고 40%를 유동시총으로 재배분(합성 테스트로 상한·R2 유지 확인).
- `etf/global_evidence.py` (2026-07-29) — 해외판 근거 수집기(EDGAR). SEC 제출사
  4종(MU·CAMT 10-K/20-F, TSM 20-F, ONTO 10-K)의 최신 연차보고서를 받아 판정
  카드를 `etf/evidence_global/`에 생성. 영문 키워드(대문자 약어는 대소문자 구분
  — 'tsv 파일' 오탐 방지), 감사의견 영→한 매핑, XBRL 자본총계로 자본잠식 제안,
  KRW 환산 제안값(FDR, ADR 배수 경고 포함). 원문은 `docs_cache/edgar/` 캐시.
  **카드에 근거 칸 필수** — 국내 카드 7장은 근거 미기록으로 소급 불가였던 교훈.
  실사 CSV에는 절대 쓰지 않는다(판정은 사용자). EDINET(6857.T·4004.T)·
  HKEX(0522.HK)·BESI IR은 다음 단계 — 지정 시 안내 후 스킵.
  같은 날 `src/hbm_evidence.py`의 무조건 zfill(6)('MU'→'0000MU' 헛조회)을
  normalize_code로 통일하고 해외 티커 가드 추가.
- `etf/run_global_scan.py` — 실사와 무관한 관측 지표(현지통화): 위기 4구간 MDD,
  JGHBM과 **주간** 상관(미국 종가는 한국 다음날 아침이라 일간 상관은 시차로 과소).
  실측: 주간상관 0.34(Resonac)~0.55(MU) — 분산 효과 실재. 위기 MDD -23~-55%.
  ONTO는 2019 상장이라 2018 구간 '표본부족' 표기(부분 표본으로 조용히 계산 금지).
- **phase 2 완료 (2026-07-29, `etf/run_global_phase2.py`)** — MU 포함 13종목의
  용량·CU·환노출 실측 (산출: `global_capacity/global_cu/global_fx_index.csv`):
  · **용량 1,728억, 병목 넥스틴 그대로** — 비앵커 비중이 불변이라 글로벌 확장은
    용량을 늘리지 않는다. 늘어난 건 앵커 유동성뿐(MU 허용 AUM 540조).
    위기 용량 개선 가설(74~187억 병목 완화)은 **기각** — 병목이 국내 꼬마주라서.
  · **권장 CU 20억 → 30억 상향** — MU 119만원이 하이닉스 181만원에 이어 두
    번째 고가 정수주 격자를 만들어, 20억은 교란 초과율 1.7%로 강건 탈락.
    30억(30만 좌): 총괴리 3.7bp·p95 9.2bp·300회 전부 통과.
  · **환노출은 위기에서 오히려 완충** — 원화가 위기 때 약세라 USD 자산이 자연
    헤지: MDD 기여 2018 +0.4 / 2020 +0.5 / 2022 +1.5%p (2026 조정만 -0.5%p).
    환헤지 없이 가는 쪽이 낙폭 관점에서 유리하다는 실측(소급 비중 한계 동일 —
    낙폭·상대 비교만, 수익률 주장 금지).
  · 남은 이월: 글로벌판 추적오차(NAV 재생에 환율·해외 거래비용 반영) —
    필요해지면 nav_sim에 환노출 수익률을 태운다.

**단일 정본 전환 (2026-07-29 저녁, 사용자 결정: "HBM 지수는 하나다")** —
글로벌 13종목이 유일한 정본. `hist_data.FINAL`·`run_tracking.CONSTITUENTS` 기본값
전환 + 해외 KRW 환산 레이어(`foreign_ohlcv_krw`, load_field가 자동 합류).
전 러너 같은 날 재실행: 추적오차 12.5y 갭 **78bp**(TER45+비용8+현금25, 회전율 27%)
· 용량 정규장 **1,728억**(병목 넥스틴 — MU도 위기 용량 74~187억은 못 고친다)
· CU **30억**(총괴리 4.9bp, 하이닉스·MU 고가 격자 2개) · **R1~R3 전 PASS**
(최대 한미 18.00%) · 개인 복제 100bp **1억 5,665만원**(앵커 3분할로 고가 제약
완화, 12종목 1.68억보다 감소) · JGHBM parquet 글로벌판으로 재수출(12.5y +4,458%,
MDD -44.3%) · 설계서 PDF 글로벌판 재생성. 12종목 시절 수치는 역사 기록으로 본문에 보존.

## 리밸런싱 절차 — 보고 → 승인 → 반영 (2026-07-29 사용자 결정)

리밸런싱 결과는 **제안**일 뿐이다. 사용자가 오케이한 것만 정본에 들어간다
(실사의 "판정 주체는 사용자"와 같은 원리). 엔진이 정본을 직접 바꾸는 경로는 없다.

```powershell
# 1단계 [보고] — 판정 CSV 갱신(재실사·시총) 후. 정본 무접촉.
.venv\Scripts\python.exe etfun_rebalance_review.py
#   → etf/output/rebalance_proposal_<날짜>.csv (+ _diff.csv)
#   diff: 신규/편출/군이동/버퍼유지(⚠표시)/비중Δ · 회전율 · 순도 변화
# 2단계 [승인] — 사용자가 보고서 읽고 오케이
# 3단계 [반영]
.venv\Scripts\python.exe etfun_rebalance_review.py --approve etf/output/rebalance_proposal_<날짜>.csv
#   → data/processed/구성표_확정_<날짜>.csv 승격 + 체크리스트 출력
#     (상수 전환 → 러너 전체 재실행 → export_basket → 커밋)
```

- 선정은 `rebalance.select_from_selection`(팀 v2) — **히스테리시스 버퍼 작동**
  (기존 종목은 hold_core 27%/hold_sat 67%로 완화 판정, `--buffer`로 정책 선택).
  신규 문턱만으로 재선정하면 경계 종목이 과잉 편출된다. 사전 스크린은 버퍼보다 우선.
- 입력이 정본: 판정 CSV(국내 실사확정 + 글로벌 실사)에 적힌 것만 믿는다.
  재실사·유동시총 갱신 = 판정 CSV 갱신. 판정 불변이면 제안 = 현행(변경 0건) —
  기준선 검증 완료.
- `select_from_selection`의 무조건 zfill을 숫자 한정으로 최소 수정(2026-07-29,
  'MU'→'0000MU'면 기존 대조가 깨져 버퍼 무력화 — hbm_evidence와 같은 사고 클래스,
  팀 레포 diff 1건).

## 바스켓 인계 (`etf/export_basket.py`) — 정가 유니버스 전량 교체 (2026-07-29 사용자 결정)

리밸런싱 후 정가(운용 시스템) 유니버스의 HBM_etf 슬롯은 확정 구성으로 **통째로
교체**한다. 인터페이스는 `D:\data\_index\JGHBM_basket.csv` + `_meta.csv`
(공용 데이터 루트 원칙 — 레포 간 직접 import 금지).

- **전량 교체는 '목표'의 교체다. 주문은 diff로 낸다** — 전량 매도·재매수는
  리밸당 편도 100%(연 200%)로 왕복 30bp 기준 연 60bp 드래그. 현행 drift
  되돌림(연 ~27%, ~8bp)의 7배가 넘어 버퍼 정책 설계를 무효화한다.
- 소비자 계약(부분 채택 금지·sha256 무결성·시행일 종가 적용·해외 미지원 시
  전체 거부)은 export_basket.py 도크스트링이 정본.
- fail-closed: 비중 합·중복·등록부 밖 해외 티커(원화 주문 사고) 검증 실패 시
  기존 파일 불변. 쓰기는 원자적(tmp→os.replace).
- 정기변경 인계 때는 `--effective 정기변경일`을 명시할 것 (생략 시 오늘).
- **승인 게이트 뒤에서만 실행한다** — 위 리밸런싱 절차 3단계의 마지막. 제안 상태에서 인계 금지.

## 지수 내보내기 (`etf/export_index.py`)

이 ETF를 tech_dashboard에서 **하나의 종목처럼** 보기 위해, 지수를 OHLCV parquet로
`D:\data\_index\JGHBM.parquet`에 내보낸다. 레포끼리 직접 import하지 않고 공용
데이터 루트를 인터페이스로 쓴다 — 한쪽이 바뀌어도 다른 쪽이 깨지지 않는다.

지수는 종가뿐이라 나머지는 합성한다:

```
Close  = 지수 레벨 (분기 재고정, PIT 비중)
O/H/L  = 구성종목의 (해당가/종가) 비율을 비중가중 → 지수 종가에 적용
Volume = 가중 거래대금 ÷ 지수 레벨 (OBV·MFI 같은 거래량 지표용 유사값)
```

고가·저가를 구성종목 고가·저가의 **가중합으로 직접** 만들면 안 된다. 종목마다 고점
시각이 달라 동시에 성립하지 않는 값이 되고 지수 변동폭이 부풀려진다. 그래서 비율을
가중평균한 뒤 종가에 곱하고, 마지막에 High ≥ max(O,C) · Low ≤ min(O,C)로 정합화한다.
